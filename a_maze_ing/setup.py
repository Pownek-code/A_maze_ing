from setuptools import setup

setup(
    name="mazegen",
    version="1.0.0",
    packages=["mazegen"],
    description="A Python module for algorithmic maze generation and graph traversal.",
    author="schanqou",
    python_requires=">=3.10",
)